import React from "react";
import { Card, Typography, Divider, Button } from "antd";

const { Title, Text, Paragraph } = Typography;

interface Props {
  bookings: any;
}

export const ListingBooking = ({ bookings }: Props) => {
  return <div></div>;
};
